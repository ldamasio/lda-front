# lda-front workspace

Frontend pessoal de Leandro Damasio.

- App ativo: `lda-front/`
- Stack: SvelteKit + TypeScript
- Idiomas: PT-BR em `leandrodamasio.rbx.ia.br`, EN em `leandrodamasio.rbxsystems.ch`, demais locais via prefixo de path (`/pt`, `/de`, `/es`, `/fr`, `/it`, `/zh`)
- Conteúdo editorial: home, blog e notas carregados server-side de Contabo S3 (`rbx-content`) sem fallback local
- Contato: formulário via RBX Comms + WhatsApp, sem alterar o copy literal do site

Referência histórica do design system: `Leandro Damasio Design System/`
